# 2026-09-18 工作日志

## 为上传 GitHub 重写 Git 过滤规则

**背景**：项目工作区约 8GB，其中真正的源码/文档仅 25MB。原 `.gitignore` 已有基础规则但不够系统。

**产出**
- 重写 `D:\pythonpro\LocalStock-unified\.gitignore`（10 个分类分区，约 130 行）
- 新增 `D:\pythonpro\LocalStock-unified\.gitattributes`（换行符 / 二进制 / linguist 规则）

**关键结论（后续不要重复踩）**
- 入库 payload：**804 个文件 / 25.2MB**，无文件 ≥50MB（GitHub 100MB 硬限无风险）。
- 体积大头全部已挡在库外：`data/market/*.db`(6.3G)、`services/joinquant-local/data/`(6.4G)、
  `apps/desktop/node_modules`(721M)、`apps/desktop/dist`(561M)、`data/runtime/`(213M)、
  `data/snapshots/`(27M)。
- ⚠️ **陷阱**：`apps/desktop/build/icon.png` 是 electron-builder 打包资源，**必须入库**。
  因此不能写裸 `build/` 规则（git 无法重新包含被排除目录的子文件），必须用 `build/*` + `!apps/desktop/build/icon.png`。
- 用户决定：`services/joinquant-local/数据同步.exe`(6.9M) 与每日选股 skill 打包 zip **保留跟踪**；
  **不**设置全局 `*.exe` / `*.zip` / `*.db` 忽略规则（仅按路径精确忽略，注释里留了开关）。
- `knowledge/`（66MB：strategy-library 996 + project-history 365 + conversations 16）默认仍排除，
  .gitignore 末尾三行注释即为开关。用户表示这部分自己上传。
- 密钥扫描通过：无 sk- 密钥、无私钥块、无真实邮箱。
  - `apps/desktop/electron/market/eastmoney.ts` 的 `SEARCH_TOKEN='D43BF722...'` 是东财公开前端
    查询 token，非私人凭据，保留无妨。
  - `pa-agent/pa_agent/ai/workbuddy_connector.py` 从本地文件动态读取 token，源码内无硬编码密钥。
- `core.autocrlf=true`，为**避免对已入库 505 个文件造成全量换行符重写**，
  `.gitattributes` 刻意未设全局 `* text=auto`，只对 `.bat/.cmd/.ps1`(CRLF) 与 `.sh`(LF) 显式声明。
- `.claude/`、`.idea/` 已忽略；`CLAUDE.md`、`AGENTS.md`、`启动本项目.bat`、`docs/项目介绍.md` 入库。

**仓库状态**：`master` 分支，已有 3 个提交，**尚未配置 remote**（用户自行推送）。

## 补充：核对聚宽服务与 pa-agent 是否完整入库（同日）

**结论**：两者都在待上传清单中，完整无损。
- `services/joinquant-local/` → **273 个 / 20.83 MB**（docs/project-docs 154、scripts 51、
  engine 3、server 3、pages 3、api 1，含 `数据同步.exe` 6.9M、LICENSE、HANDOVER.md）
- `apps/desktop/python/` → **295 个 / 3.29 MB**（pa-agent/pa_agent 89、
  pa-agent-aggressive/pa_agent 105、两侧 prompt_engineering 共 59、pa_service 各 3、config 示例 3）

**修正了一处规则过严**（原根 .gitignore 写的 `services/joinquant-local/data/` 整体排除）：
该目录里有 460KB 有价值的小文件被误吞，已改为按类型精确排除。
- 恢复入库：`stock_industry.json`(229K)、`picks_history.json`、`backtest_history.json`、
  `picks_track_history.json`、`sync_retry.sh`、`make_skill_zip.py`、`.gitkeep`、3 个 vt_kline png
- 仍然排除：`stock_data.db`(6.3G)、`backtest.db`(4.4M)、`backtest.db-wal`(6.2M)、
  `research_panel.pkl`(101M)、`*.log`、`sync_meta.json`（后者由 joinquant 自带的嵌套 .gitignore 排除）
- 修正后总量：**816 个 / 25.7 MB**

**⚠️ 多层 .gitignore 优先级坑（重要）**：`services/joinquant-local/.gitignore` 是**嵌套**规则，
按 git 规则**深层优先于根层**，因此根 .gitignore 里的 `!` 反排除**无法**覆盖嵌套规则。
要放行 `数据同步.spec`（PyInstaller 打包 `launcher.py` → 已入库的 `数据同步.exe` 的配方，建议保留）
或 `scripts/test_*.py`（已确认是 `# Test Chinese` 之类的编码调试废脚本，无需保留），
必须直接改 `services/joinquant-local/.gitignore` 本身。目前未改动，待用户决定。

**排查用命令**：`git check-ignore -v <路径>` 可定位到底是哪一层、哪一行的规则命中。

## 补充：补齐 requirements.txt 与新建云端数据来源文档（同日）

**用户决策**：① 云端数据来源要**新建一份汇总文档**（不是指已有文档）；② 依赖**补齐所有项目但保留原有设计**（必需项不动，实际 import 到的可选源以注释形式列出）。

**新增 4 份 + 更新 2 份**
- 新增 `services/joinquant-local/requirements.txt`：pandas / numpy / baostock / matplotlib（可选 pyinstaller）
- 新增 `apps/desktop/python/scripts/requirements.txt`：akshare / pandas（仅全量下载脚本，不进 venv）
- 新增 `apps/desktop/python/engine/requirements.txt`：**零第三方依赖**，纯标准库，文件本身即"约束声明"
- 更新 `pa-agent/requirements.txt`、`pa-agent-aggressive/requirements.txt`：保留必需 4 项，补注释版可选源
- 新增 `docs/云端数据来源.md`（13.9KB，35 个云端主机汇总）

**关键技术事实（后续复用，别再重新查）**
- **pa-agent 与 pa-agent-aggressive 共用同一个 venv**：`<userData>/pa-agent-venv`
  = 仓库内 `data/runtime/desktop/pa-agent-venv`，由 `electron/pa/runtime.ts` 自动创建，
  **不读 requirements.txt 之外的东西**。实测只装了 4 个顶层包：
  openai 3.13.0 / pydantic 2.13.5 / jsonschema 4.26.0 / tzdata 2026.3（+ 13 个传递依赖）。
  → 这反证了 akshare/baostock/pandas/PyQt6/cursor_sdk 等 import 都在**非服务端可达路径**。
- **稳定版已清理 PyQt6，激进版没清干净**：aggressive 里 `pa_agent/main.py`、
  `pa_agent/demo/replayer.py`、`pa_agent/data/refresh_loop.py` 三个文件是 PyQt6 **真实硬导入**；
  但 grep `refresh_loop|demo.replayer|pa_agent.main` 在 pa_service/、orchestrator/、ai/ 下**零命中**
  → 服务端不加载，故列为可选。稳定版只有注释提到 PyQt6，已是纯 Python Signal 实现。
- `jqdata` / `jqfactor` / `jqlib` / `kuanke` 是**聚宽研究环境内部模块，不在 PyPI**，
  `engine/jqcompat.py` 用 `_mk_module` 建了空桩，**不要 pip 安装**。
- 聚宽侧主数据源（腾讯/新浪）**只用标准库 urllib**，所以 joinquant 的 requirements 里**没有 requests**。
- `services/joinquant-local/数据同步.exe` 由 `数据同步.spec` + `launcher.py` 经 PyInstaller 产出。
- 云端主机共 **35 个**；东财 searchapi 的 `D43BF722...` 是公开前端 token 非私人凭据。

**Git 现状**：入库总量 **821 个文件 / 25.8 MB**，仍未配置 remote。


