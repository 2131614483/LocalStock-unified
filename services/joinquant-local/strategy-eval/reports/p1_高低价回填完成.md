# P1 高低价回填完成报告（2026-08-26）

## 执行结果

脚本：`scripts/p1_backfill_highlow.py`（腾讯 fqkline bfq，只 UPDATE NULL 的 high/low）
日志：`logs/p1_hl_backfill.log`；断点文件 `logs/p1_hl_done.json`
总耗时：三轮运行合计 ~5 小时（含两次 WAF 封禁等待）

| 轮次 | 并发 | 结果 |
|---|---|---|
| 第 1 轮 | 6 线程×0.15s | 443 只后被腾讯 WAF 封（8 分钟触发），失败重试 |
| 第 2 轮 | 2 线程×1.0s | 又封（90 秒内），再降速 |
| 第 3 轮 | **1 线程×2.0s** | **4845 只全部成功，4.1 小时跑完，零封禁** |

**最终数据**：
- 2015 年以来 high/low 已填 **11,549,482 行**（原 NULL 1153.9 万行）
- 剩余 NULL 仅 ~2.5 万行，构成：
  - 北交所股票（92/83 开头）324 只 × 全部历史 ≈ 2.28 万行——腾讯无北交所数据；引擎 `get_all_stocks` 本就排除北交所，不影响评测
  - 退市股尾部数据（000970 中科三环等，2017-2018 退市）——腾讯无退市股历史
  - 价格守门员拒收的临界行（如 2016-01-04 熔断日 CSMAR 收盘 11.33 vs 腾讯 11.30，差值超容差被正确拒绝）约 2400 行（0.02%）

## 校验记录

- 单元口径：腾讯 volume 手×100=股，与库一致（000001 三日全对）
- 列序：`[date, open, close, high, low, volume]`，已按 o/c/h/l 正确映射
- 价格交叉核对：每行 `|tx_close − db_close| > max(0.011, db_close×0.001)` 即拒收（共拒 ~2400 行，宁缺毋滥）
- 抽样：600519 近四日 h/l 与东财网页行情一致 ✓

## WAF 经验（重要，后续脚本沿用）

- 腾讯 web.ifzq.gtimg.cn 阈值≈**千级请求/短窗口**：6 并发 8 分钟被封、2 并发 90 秒被封、单线程 2s 间隔（~0.35 req/s）稳定跑 4 小时
- 封禁形态：返回 waf.tencent.com 501 页面或空响应；冷却时间 **20~30 分钟**
- 断点续传设计有效：fail_fetch 不写 done 集合，重启自动补

## 引擎侧待办（P1 后半）

- [ ] 除息日分红入账（div = pre_close_prev×dretwd − (close−pre_close)）
- [ ] 涨跌停推导 + 涨跌停禁止成交约束
- [ ] attribute_history/get_price 的 high/low 用真实值
