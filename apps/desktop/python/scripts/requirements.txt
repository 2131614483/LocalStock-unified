# ==============================================================================
# apps/desktop/python/scripts —— 全量历史数据下载脚本依赖
# ------------------------------------------------------------------------------
# 适用范围：本目录下的独立下载脚本（非应用运行时依赖）。
#
# 重要：这些依赖**不进入桌面端自动创建的 pa-agent venv**，
#      仅在你需要手工跑全量数据下载时才安装。
#      桌面端「回测页 → 下载数据」走的是同一套脚本，但独立准备环境。
#
# 安装方式：pip install -r requirements.txt
# ==============================================================================

akshare>=1.14
#   download_full_data.py 的唯一数据源（AkShare 新浪源，免费免注册）。
#   用途：全 A 股（沪深约 5,206 只）× 1990~最新 前复权日线全量下载，
#        建表 stock_daily / trade_calendar / stocks / index_daily。
#   ⚠️ 为什么不选 baostock：本机网络实测单只 >120s、频繁卡死，已弃用。

pandas>=2.2
#   akshare 返回 DataFrame，需要 pandas 处理与落库。

# ---------- 说明 ----------
# 1) 全量下载约 1.5~2 小时，支持断点续传（已有数据自动跳过）。
# 2) templates/ 下的 22 个策略模板（dual_ma / bollinger / turtle 等）
#    **只用标准库**，运行时的 data/draw_* 等 API 由桌面端注入，
#    因此不需要任何第三方依赖，也不应被当作独立脚本直接执行。
# 3) 数据源与下载流程的完整说明见 apps/desktop/docs/下载数据源.md
